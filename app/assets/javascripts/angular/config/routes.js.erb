Bidly.config(["$stateProvider", "$httpProvider", "$urlRouterProvider", "$locationProvider",
  function($stateProvider, $httpProvider, $urlRouterProvider, $locationProvider) {
  $stateProvider

    .state('nav', {
      abstract: true,
      templateUrl: 'shared/nav.html',
      controller: 'NavController'
    })

    .state('nav.blaster', {
      abstract: true,
      views: {
        'content': {
          templateUrl: 'blaster/menuHome.html',
        }
      },
    })

    .state('nav.blaster.menu', {
      url: '/blaster',
      views: {
        'content': {
          templateUrl: 'blaster/menuList.html',
          controller: 'BlasterController',
          
        }
      }
    })

    .state('nav.blaster.submenu', {
      url: '/blaster/:qwe',
      views: {
        'content': {
          templateUrl: 'blaster/menuList.html',
          controller: 'BlasterController',
        }
      }
    })

    .state('nav.blaster.mycart', {
      url: '/mycart',
      views: {
        'content': {
          templateUrl: 'blaster/cartList.html',
          controller: 'ShowCartController',
        }
      }
    })

    .state('nav.blaster.checkout', {
      url: '/checkout',
      views: {
        'content': {
          templateUrl: 'blaster/checkoutList.html',
          controller: 'CheckoutController',
        }
      }
    })


    .state('nav.items', {
      url: '/',
      views: {
        'content': {
          templateUrl: 'items/items.html',
          controller: 'ItemController'
        }
      }
    })

    .state('nav.show', {
      url: '/items/show',
      views: {
        'content': {
          templateUrl: 'items/show.html',
          controller: 'ItemController'
        }
      }
    })

    .state('nav.dashboard', {
      url: '/dashboard',
      views: {
        'content': {
          templateUrl: 'users/dashboard.html',
          controller: 'DashboardController'
        }
      }
    })

    .state('nav.login', {
      url: '/login',
      views: {
        'content': {
          templateUrl: 'users/login.html',
          controller: 'LoginController'
        }
      }
    })

    .state('nav.register', {
      url: '/register',
      views: {
        'content': {
          templateUrl: 'users/register.html',
          controller: 'LoginController'
        }
      }
    })

    .state('nav.how', {
      url: '/how',
      views: {
        'content': {
          templateUrl: 'pages/how.html',
        }
      }
    })


    $httpProvider.defaults.withCredentials = true;
    $urlRouterProvider.otherwise('/');
    $locationProvider.html5Mode({
  enabled: true,
  requireBase: false
});

  }]).run(["$rootScope", "$state", function($rootScope, $state) {
      $rootScope.$state = $state;
    }]);
